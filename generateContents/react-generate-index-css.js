const generateIndexCSS = () => {
  let code = `body {\n`;
  code += `  margin: 0;\n`;
  code += `  padding: 0;\n`;
  code += `  font-family: sans-serif;\n`;
  code += `}`;
  return code;
}

export default generateIndexCSS;
